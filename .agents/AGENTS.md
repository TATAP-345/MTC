# Project Rules

- Always stage (`git add .`), commit with a descriptive message, and push (`git push origin <branch>`) all changes to GitHub automatically after completing any file modifications, without waiting for an explicit "push" command from the user.
